"use client";

/**
 * Hook conveniente para acceder al contexto de autenticación.
 * Alias de useAuthContext() con un nombre más corto.
 */
export { useAuthContext as useAuth } from "@/contexts/AuthContext";
